// Re-exported from the canonical shadcn util location so both
// `./utils` (used by these components) and `@/lib/utils` (shadcn CLI default)
// resolve to a single source of truth.
export { cn } from "../../lib/utils";
